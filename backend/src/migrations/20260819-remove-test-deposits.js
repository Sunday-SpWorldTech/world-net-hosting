const mongoose=require('mongoose');require('dotenv').config();const Wallet=require('../models/Wallet');
(async()=>{try{await mongoose.connect(process.env.MONGODB_URI);let wallets=0,removed=0;for await(const wallet of Wallet.find().cursor()){const before=wallet.transactions.length;wallet.transactions=wallet.transactions.filter(t=>!(/\b(test|demo|sandbox)\b/i.test(String(t.description||''))||/^(TEST|DEMO|SANDBOX)[-_]/i.test(String(t.reference||''))));const n=before-wallet.transactions.length;if(n){removed+=n;wallets++;await wallet.save();}}console.log(JSON.stringify({walletsUpdated:wallets,testTransactionsRemoved:removed}));await mongoose.disconnect();}catch(e){console.error(e);process.exit(1)}})();
